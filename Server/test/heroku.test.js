//var request = require('supertest');
//const expect = require('chai').expect;

// This should only ping the heroku server

/*
describe('Try REGISTER route (/accout/register)', () => {

    it('It should send an error, the user already exist in the db', (done) => {
      request('https://epitrello-server.herokuapp.com')
      .post('/account/register')
      .set('Accept', 'application/json')
      .send({ email: 'template@template.template', password: 'test1234', pseudo: 'template' })
      .expect('Content-Type', /json/)
      .expect(400)
      .expect(res => {
        expect(res.body.status).to.equal('ERROR');
        expect(res.body.data).to.equal('Pseudo already exist');
      })
      .end(done);
    });

});
*/
